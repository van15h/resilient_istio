docker build -t cpanel .


use "docker-compose up" from folder "services" to start the preconfigured system.
image will be built automatically, if image is not found in your system.
see "services/docker-compose.yml" for more details.